{
  "name": "eacs-attendance-monitoring",
  "version": "1.0.0",
  "description": "A comprehensive attendance monitoring and management system",
  "main": "index.js",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "setup": "npm run install-all && node setup-env.js",
    "setup-env": "node setup-env.js"
  },
  "keywords": [
    "attendance",
    "monitoring",
    "react",
    "nodejs",
    "sql-server",
    "dashboard"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "react-chartjs-2": "^5.3.0"
  }
}
